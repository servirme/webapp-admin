language: node_js
node_js:
  - 8
cache:
  - node_modules

before_install:
  - npm set progress=false

script:
  - npm run lint
  - npm run test

after_script:
  - npm run send-coverage-data

#deploy:
#  provider: script
#  script: npm run deploy-prod
#  on:
#    branch: master

